current status:

implemented:
Detection of checkerboard
masking of region of interest (manually and via the checkerboard)
computation of trafo s.t. ROI is in x-y-Plane

computation of Projectionmatrix via cv::SVD

see imgs!



